# dependencies & caches (not part of the snapshot)
node_modules/
.cache/
__pycache__/
*.pyc
.DS_Store

# the ~70 MB of upstream tokenizer files fetched by tools/fetch_models.py —
# regenerate with `python3 tools/fetch_models.py`, never commit them
tools/raw/

# local test artefacts
*.log
/node_modules

# promo render intermediates are ignored, but the final video + filmstrip
# are committed so people can grab the video straight from the repo.
# regenerate everything with: python3 promo/generate_video.py
promo/renders/*
!promo/renders/mlbox_promo_30s.mp4
!promo/renders/filmstrip.jpg
promo/assets/
